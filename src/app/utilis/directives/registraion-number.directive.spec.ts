import { RegistraionNumberDirective } from './registraion-number.directive';

describe('RegistraionNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new RegistraionNumberDirective();
    expect(directive).toBeTruthy();
  });
});
