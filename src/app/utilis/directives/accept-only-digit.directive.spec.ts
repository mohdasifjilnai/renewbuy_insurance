import { AcceptOnlyDigitDirective } from './accept-only-digit.directive';

describe('AcceptOnlyDigitDirective', () => {
  it('should create an instance', () => {
    const directive = new AcceptOnlyDigitDirective();
    expect(directive).toBeTruthy();
  });
});
