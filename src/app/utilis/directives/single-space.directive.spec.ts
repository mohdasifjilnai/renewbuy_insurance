import { SingleSpaceDirective } from './single-space.directive';

describe('SingleSpaceDirective', () => {
  it('should create an instance', () => {
    const directive = new SingleSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
